{
  "name": "root",
  "private": true,
  "license": "AGPL-3.0-only",
  "engines": {
    "npm": ">=8.5.5",
    "node": ">=16.15.0"
  },
  "scripts": {
    "dev": "pnpm --filter app run dev",
    "build": "pnpm turbo build",
    "hardhat:node": "pnpm --filter contracts run node",
    "hardhat:test": "pnpm --filter contracts run test",
    "generate-abi": "pnpm --filter contracts run generate-abi",
    "generate-types": "pnpm --filter app run generate-types"
  },
  "dependencies": {
    "turbo": "^1.7.4"
  },
  "packageManager": "^pnpm@7.0.0"
}
